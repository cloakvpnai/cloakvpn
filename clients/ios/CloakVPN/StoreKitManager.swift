// SPDX-License-Identifier: MIT
//
// RETIRED — StoreKit in-app purchase is no longer used.
//
// Lattice VPN sells subscriptions on the website (latticevpn.ai) and the
// app authenticates with the customer's account number. In-app purchase
// would take Apple's 15–30% cut and tie every customer to an Apple ID,
// defeating the no-account privacy model — see docs/BILLING_INTEGRATION.md
// §9. Provisioning now goes through LatticeAccountClient against the
// central cloakvpn-api.
//
// This file is intentionally empty. It can be deleted from the project
// (the Xcode group is file-system synchronized, so removing the file is
// all that's needed).
